
"""
in python comment is start with #
print('hello world') #can also be used to
prevent Python from executing code:
"""
"""
Since Python will ignore string literals that are not assigned to a variable, you can
add a multiline string (triple quotes) in your code, and place your comment inside it:

"""
print('hello world') #comment can be placed at the end of the lline

def myfunc():
     kar
    kar=5
    print(kar)
myfunc()
print(kar) #program not execute because kar is a local variable